[**@microblink/blinkid-ux-manager**](../README.md)

***

[@microblink/blinkid-ux-manager](../README.md) / createBlinkIdUxManager

# Function: createBlinkIdUxManager()

> **createBlinkIdUxManager**(`cameraManager`, `scanningSession`, `options`): `Promise`\<[`BlinkIdUxManager`](../interfaces/BlinkIdUxManager.md)\>

Creates a BlinkIdUxManager.

## Parameters

### cameraManager

`CameraManager`

The camera manager.

### scanningSession

`RemoteScanningSession`

The scanning session.

### options

[`BlinkIdUxManagerOptions`](../type-aliases/BlinkIdUxManagerOptions.md) = `{}`

## Returns

`Promise`\<[`BlinkIdUxManager`](../interfaces/BlinkIdUxManager.md)\>

The BlinkIdUxManager instance.
