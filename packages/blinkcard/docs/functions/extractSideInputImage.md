[**@microblink/blinkcard**](../README.md)

***

[@microblink/blinkcard](../README.md) / extractSideInputImage

# Function: extractSideInputImage()

> **extractSideInputImage**(`scanningResult`, `side`): `null` \| `ImageData`

Extracts the input image for a given side from the scanning result.

## Parameters

### scanningResult

[`BlinkCardScanningResult`](../type-aliases/BlinkCardScanningResult.md)

The scanning result.

### side

[`ScanningSide`](../type-aliases/ScanningSide.md)

The side to extract the input image for.

## Returns

`null` \| `ImageData`

The input image for the given side.
