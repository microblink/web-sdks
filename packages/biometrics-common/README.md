# @microblink/biometrics-common

This is a private workspace package for types and utilities shared by Biometrics Core and the Biometrics Worker. Applications use `@microblink/biometrics-core` or `@microblink/biometrics` instead.
